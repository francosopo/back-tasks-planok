services:
db:
  image: postgres:latest
  environment:
    POSTGRES_USER: planok
    POSTGRES_PASSWORD: planok
    POSTGRES_DB: tasks
  ports:
    - "5432:5432"

web:
  build: .
  command: python manage.py runserver
  volumes:
    - .:/app
  ports:
    - "8000:8000"
  depends_on:
    - db 

